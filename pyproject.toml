[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "AdSERP"
version = "1.0.0"
dependencies = [
    "opencv-python",
    "numpy",
    "pandas",
    "selenium",
]

[tool.hatch.build.targets.wheel]
packages = ["adserp"]